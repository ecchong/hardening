---
- hosts: linux
  tasks:

  - name: Shutdown firewall
    service:
      name: firewalld
      state: stopped
      enabled: false

  - name: Install insecure pkgs
    yum:
      name: telnet
      state: installed
